# Комплексный план усиления «ИИ: монтаж» (2026-07-10)

> Синтез из:
> - [PremiereGPT teardown](2026-07-10-premieregpt-teardown.md) — что перенять у конкурента.
> - [Аудит нашего плагина](2026-07-10-our-plugin-audit.md) — что чинить у себя.
>
> Философия: **усиливаем локально-первичную приватную архитектуру** (наш RU-moat), НЕ копируем
> облачную зависимость PremiereGPT. Берём их UX-паттерны и фичи, реализуя их на своём стеке
> (Cloud.ru + локальный host + ffmpeg).

## Где они объективно лучше (и почему)
1. **Vision** — судят монтаж по картинке, не только по тексту. Для нас, чья цель — «монтаж по
   смыслам», это прямой пробел: смысл часто в кадре, а не в словах.
2. **Устойчивость длинных операций** — WS + heartbeat + reconnect. У нас длинные вызовы —
   fire-and-forget через CDP-костыль, в проде — таймауты клиента.
3. **UX ошибок** — доменные коды + человеко-читаемые инструкции. У нас generic.
4. **Ширина фич** — джамп-каты, диаризация, авто-вертикаль, b-roll, цветокор, смарт-субтитры.
5. **Прозрачность стоимости** — контекст-поповер (% окна, system vs actions).

## Где мы лучше (сохранить как moat)
- Приватность (ничего не уходит в облако кроме LLM/Whisper-вызовов на Cloud.ru).
- Мгновенный host (код на диске, не round-trip на каждый tool-call).
- Самовалидация деструктивных host-операций (reject, не clamp) — у них этого нет.
- Заточка под RU-контур (Cloud.ru тарифы, ₽-счётчик).

---

## Дорожная карта — 3 волны

### 🌊 Волна 1 — Надёжность (чиним свои дыры; блокер перед новыми фичами)
Приоритет по подтверждённому/дешёвому:
1. **429 `Retry-After`** ✅ **СДЕЛАНО 10.07** — parseRetryAfterMs + wait=max(backoff, header, cap 60с).
2. **Пересечения интервалов** ✅ **СДЕЛАНО 10.07** — JS-валидатор уже ловил (P1-F); host
   applyTranscriptCuts теперь отклоняет весь план ДО beginUndoGroup. Live-подтверждено.
   🆕 Бонус: закрыт live-обнаруженный P0 «24ч-wrap» — out-of-bounds время резало реальный
   контент; `_seqEndSec`-гард в applyTranscriptCuts + applyTimecodeEdits (см. аудит, статус-секция).
3. **Пустые Whisper-segments** ✅ **СДЕЛАНО 10.07** — assertNonEmptyTranscript на выходе runFromPrep.
4. **Cleanup temp-файлов при abort** ✅ **СДЕЛАНО 10.07** — 4 утечки (extract-пул, clip_queue,
   nest-чанки, media_file tmp) → finally/catch-очистка + самоудаление in-flight чанков; 4 теста.
5. **`_pendingProposal` / смена секвенции** ✅ **СДЕЛАНО 10.07** — click-time capture идентичности
   proposal + host-side `expectedSequenceName`-гейт в applyTranscriptCuts/applyTimecodeEdits
   (live-проверено CDP). Host 2.6.9.
6. **Утечка `_transcriptCheckpoints`** ✅ **СДЕЛАНО 10.07** — удаление замещённого backupId +
   кэп 8 с вытеснением старейшего.
7. **NaN/bounds в маркерах и trackIndex** ✅ **СДЕЛАНО 10.07** — negative timeSec reject в host;
   NaN-гарды в интервалах; trackIndex-пункты аудита оказались stale (уже безопасны).
8. **Тесты** ✅ **СДЕЛАНО 10.07** — пересечения/abort-SSE/падающий-:250 оказались stale (уже
   покрыты); добавлено: пустая секвенция (9 тестов), localStorage недоступен (7 тестов + фикс
   setMessages/clearChat без try/catch); смена секвенции = live-e2e CDP из п.5. 620/620.
   **Волна 1 закрыта.**
> Каждый пункт — сначала грепом подтвердить факт (см. VERIFY-метки в аудите), потом
> failing-тест → фикс → e2e через CDP на реальной секвенции.

### 🌊 Волна 2 — UX-паритет (дёшево, большой эффект)
1. **Человеко-читаемые ошибки** ✅ **СДЕЛАНО 10.07** — каталог вынесен в
   `client/shared/error-humanizer.js` (kind: cancel/auth/model/payload/quota/server/tls/network/
   ffmpeg/media; первый матч побеждает; word-boundary против ложных 413/429). Все три точки показа
   (чат, транскрипция, tools-таб) идут через `_classifyError` → делегат в ErrorHumanizer.
   Retry транскрипции расширен на server (5xx). 30 тестов (суммарно 650/650), live-проверка CDP.
2. **Контекст/токен-поповер** ✅ **СДЕЛАНО 10.07** — клик по usage-badge открывает поповер:
   % окна контекста последнего запроса (ctxTokens в pricing-карте fm-defaults), прогресс-бар
   (зел/жёлт/красн на 50/80%), разбивка «система+инструменты» (baseline = min prompt по модели
   за сессию) vs «диалог+результаты», итоги сессии, подсказка «начните новый чат» с 50%.
   UsageMeter.getContextInfo() + 8 тестов (658/658), live-смоук CDP (59% на 120K prompt).
3. **Стек чекпоинтов** ✅ **СДЕЛАНО 10.07** — lastUndo в ContextStore стал стеком (LS-ключ
   прежний, одиночный формат мигрирует при чтении; cap 8, вытесненные возвращаются для чистки
   транскрипт-снимков). getLastUndo = вершина (старый контракт цел), getUndoStack/removeUndoEntry/
   clearUndoStack — новые. Кнопка отката при >1 точке открывает поповер со списком (новые сверху,
   метка времени); откат из середины — removeUndoEntry(ts). 8 тестов (666/666), live-смоук CDP.
4. ~~**Пресеты экспорта `.epr`**~~ ❌ **ЗАКРЫТО 10.07 как неактуальное** — live-проверка CDP на
   PP 26.2.2: `exportAsMediaDirect` возвращает «Unknown Error» даже с настоящим user-сохранённым
   .epr (Documents/AME/26.0/Placeholder Preset.epr, 35KB) — гипотеза «системные пресеты виноваты»
   опровергнута, сломан сам API. Цель пункта (моно/low-res, меньше лимита API) уже достигнута
   ffmpeg-путём: libmp3lame 64k mono 16kHz ≈ 0.5 МБ/мин (transcribeChunkFormat в fm-defaults).
   Вернуться к .epr только если Adobe починит экспорт (или после UXP-миграции).

### 🌊 Волна 3 — Новые возможности (по ценности для «монтажа по смыслам»)
> ~~❌ Решение пользователя 10.07: vision по кадрам и смарт-субтитры НЕ делаем.~~
> ✅ **11.07 пользователь ВЕРНУЛ оба пункта в объём** — vision (п.1) и смарт-субтитры (п.4).
1. **⭐ Vision по кадрам** — 🔄 В РАБОТЕ 11.07 (возвращён). Физибилити live-проверено:
   Cloud.ru `/v1/models` вырос до 70 моделей; внешние вендоры (anthropic/openai/gemini/GLM)
   отдают `403 RBAC: Access Denied`; **vision работает на Qwen/Qwen3.5-397B-A17B и
   MiniMaxAI/MiniMax-M3** (обе верно описали тест-картинку через OpenAI-style
   `content:[{type:'image_url',…}]`). Источник кадров: ffmpeg (BRAW НЕ декодирует —
   «no decoder found»; QE `exportFramePNG/JPEG` на PP 26.2.2 всегда false — экспортёр
   сломан как и exportAsMediaDirect) → для BRAW-проектов нужен decodable-фоллбэк
   (черновой экспорт/прокси). План: экстрактор кадров + инструмент агента
   `describe_frames` + цены Qwen3.5/MiniMax в pricing-карту.
2. ~~**Джамп-каты по тишине**~~ ✅ **ЗАКРЫТО 10.07 — УЖЕ РЕАЛИЗОВАНО** (пункт плана оказался stale):
   в tools-табе с 26.06 есть карточки «🔇 Убрать тишины» (`card-silences`) и «⚡ Jump cuts»
   (`card-jumps`), обе `needs-audio` — работают по RMS-таймлайну без Whisper через «⚡ Анализ аудио»
   (`AudioPreprocess.analyzeAll`: ffmpeg silencedetect + ebur128 + RMS 0.05с). Детект —
   `DeterministicPipelines.cutSilences/jumpCuts` → `silenceIntervalsFromRms` (пер-клип адаптивный
   порог P92−22дБ, padding, WYSIWYG-превью на waveform); применение — та же механика
   `applyTranscriptCuts`. Live-подтверждено CDP 10.07 на секвенции «6»: анализ → cutSilences
   (2 паузы, порог −38 dB/речь≈−16 dB) → jumpCuts (1 пауза 0.9с). Ничего дорабатывать не требуется.
3. **Диаризация мультикама по спикерам** ✅ **СДЕЛАНО 10.07** — локально, без облака (Whisper
   Cloud.ru спикеров не отдаёт): `assignSpeakersByRms` в deterministic-pipelines — средний RMS
   каждого мика в окне сегмента, лидер должен быть громче тишины (−55 дБ) и второго на маржу
   (6 дБ), неуверенные (перекрытие/тишина) без метки; O(segments+samples) по указателям.
   Host 2.7.1 `getDiarizeMicSources` (read-only): мики = не-muted дорожки с файлами (direct)
   И inner-треки вложенной секвенции (nest, через `_enumerateNestAudibleAudio`, группировка
   по inner trackIndex; разрезанный nest = несколько частей одного мика). Панель: карточка
   «🗣 Спикеры» (needs-transcript) с фильтром дорожек («4-6», «A4, A6» — parseAudioTrackFilter),
   RMS на файл считается один раз параллельно (прогресс «готово m из n»), метки пишутся в
   segment.speaker → buildStructure → entry.speakers → агент видит в get_transcript_structure.
   Тесты 701/701 (+9 assignSpeakersByRms, +4 micPartsToTimeline, +5 parseAudioTrackFilter).
   Live e2e CDP на реальном подкасте (DZ_TEST: Mic 1/Mic 2 параллельно, In–Out 0–180с, реальная
   Whisper-транскрипция 54 сегмента): размечено 50/54, Спикер 2 = ведущий (вопросы),
   Спикер 1 = гость (ответы) — сошлось с ground truth по тексту; дубли clip_queue получают
   одинаковую метку; 4 без метки = джингл/перекрытия. Негативный тест фильтра («2» → «нужно
   минимум 2 микрофона») прошёл. Секвенция DZ_TEST оставлена в проекте.
4. **Смарт-субтитры** ✅ **СДЕЛАНО 11.07** (возвращён пользователем 11.07) — caption-трек
   из Whisper-транскрипта, полностью локально. Планировщик чистый: `segmentsToSrtCues`
   (перенос жадный ≤2 строки × 42 символа, титр ≤5с; длинные сегменты бьются по словам
   с ЛИНЕЙНЫМ таймингом — Whisper пословных меток не отдаёт; `withSpeakers` ставит «— »
   на смене segment.speaker от диаризации) + `cuesToSrt` (стандартный SRT
   HH:MM:SS,mmm). Панель: карточка «💬 Субтитры» (needs-transcript, чекбокс «метки
   спикеров»), SRT пишется из Node-контекста панели в `~/.extensions_llm_chat_pr/subtitles/`
   (BOM, имя = секвенция+timestamp — item остаётся в проекте). Host 2.9.0
   `importSrtAsCaptions`: гейт `expectedSequenceName` → `importFiles` → новый item по
   diff nodeId → `seq.createCaptionTrack(item, 0)`; undo-группа. ⚠ PP 26.2.2 НЕ даёт
   read-back caption-треков (ни DOM reflect, ни QE — существует только сам createCaptionTrack);
   подтверждение существования трека только визуальное. Тесты 721/721 (+6
   segmentsToSrtCues, +2 cuesToSrt). Live CDP e2e на DZ_TEST (54 сегмента, 50 со
   спикерами): 79 титров ~3.0 мин, «— » на смене говорящего, файл в проекте; негативные
   тесты — чужая expectedSequenceName отклонена, несуществующий .srt отклонён; гейт
   needs-transcript снимается после разблокировки транскрипта.
5. **Авто-вертикаль/рефрейм для шортсов** ✅ **СДЕЛАНО 10.07** — исходник read-only:
   host 2.8.0 `applyVerticalReframe` = clone() → переименование «<имя> — 9x16» →
   `setSettings` 1080×1920 (live-проба: работает на PP 26.2.2 без QE-пресетов) → Motion
   Scale/Position per клип (компонент по matchName `AE.ADBE Motion`, свойства по displayName
   с fallback на индексы 0/1). Самовалидация ВСЕГО плана до первой мутации по исходной
   секвенции (границы индексов, scale (0,1000], pos [-5,5]) — reject, не кламп; гейт
   `expectedSequenceName`; коллизия имени клона → отказ. Планировщик чистый:
   `planVerticalReframe` (cover-скейл, горизонтальный излишек кропится позицией, окно
   кропа клампится в кадр) + `parseVerticalOffsets` («Гость: -20; Ведущий: 15» —
   substring-матч имени клипа). Vision исключён — фокус per-камера задаёт пользователь,
   для статичного подкаст-мультикама этого достаточно. Размеры исходников: ffprobe
   локально (`probeVideoDimensions`, BRAW читается — 3872×2176 полный сенсор), для
   nest-клипов host отдаёт размеры inner-секвенции (nestDims). Карточка «📱 Вертикаль
   9:16» (не needs-transcript, не proposal — откат = удалить новую секвенцию).
   Тесты 713/713 (+4 parseVerticalOffsets, +8 planVerticalReframe). Live CDP e2e на
   «6» (3840×2160, 3 BRAW): центр — scale 88.24 (=1920/2176) все 3 клипа, исходник
   нетронут (scale 100, 3840×2160); смещение «A058: -30» → posX 1.449 (сошлось с ручным
   расчётом), остальные камеры без сдвига; негативный тест коллизии имени прошёл.
   Секвенция «6 — 9x16» оставлена в проекте для визуальной проверки.
6. **Устойчивость длинных операций** — оценить локальный аналог WS-heartbeat для прод-режима
   (без CDP-костыля): держать соединение/поллинг статуса長ых вызовов Cloud.ru.
   ✅ **Остаток ЗАКРЫТ 10.07 как уже реализованный** (grep-VERIFY cloudru-client.js
   fetchWithRetry): retry ×5, экспоненциальный backoff 1/2/4/8/16с + джиттер ±20%,
   +5с на сетевые транзиенты, `Retry-After` учитывается, таймаут запроса 300с,
   per-attempt AbortController со слиянием внешнего signal. Host-сторона длинных
   операций закрыта батчевым MultiCam (ниже). Отдельный WS-heartbeat не нужен.
   ✅ **Часть 1 СДЕЛАНА 10.07 — батчевый MultiCam** (live-фейл юзера: 1.2ч подкаст →
   один evalScript на сотни сегментов → 120с-watchdog): `MulticamPlan.splitPlanIntoBatches`
   (пачки по 40 сегментов, razorTrailingEdge на стыках); host `applyMulticamCuts` — Шаг 2
   переписан с O(segments×clips) на один проход + бинарный поиск (live: 40 сегментов ≈ 2с),
   гейт `expectedSequenceName` перед КАЖДЫМ батчем, клипы вне плана не трогаются; panel —
   последовательный цикл батчей с прогрессом «батч i/n» и fail-stop («применено X из Y,
   откатите ⏪»). Host 2.7.0. Тесты 679/679; live CDP e2e на клоне «6 Copy»: 3 батча,
   стыки 80с/160с целы, чужие дорожки нетронуты, негативный тест гейта прошёл.
   ✅ **UX-пакет MultiCam СДЕЛАН 10.07:** RMS-анализ микрофонов параллельно
   (Promise.all + статус «готово m из n», ошибки атрибутируются мику «A5 (файл): …»);
   кнопка переименована «Проанализировать и предложить нарезку» (Apply — отдельный шаг);
   сводка пропозала — экранное время по камерам (mm:ss + %) и для длинных планов
   «N батчей, ≈X с» (multicamFromAudio; proposal-summary теперь pre-line). Тесты 683/683;
   live CDP смоук на реальном 4ч-агрегате «6»: 1146 сегментов, сводка
   «Общий план 19:09 (20%) · Гость 1 76:21 (80%)…, 29 батчей ≈87 с».

---

## Принципы приоритизации
- **Сначала надёжность** (Волна 1): новые фичи на дырявом фундаменте усилят боль, не ценность.
- **Мерить качеством ролика**, не «код работает» — каждую фичу e2e на реальном мультикам-материале
  (1_SYNCED и др.), с ground-truth.
- **Только сведённый вход** для монтажа по смыслам (мультикам → nest/single-cam).
- **Не жертвовать moat**: любую «облачную» фичу конкурента реализуем локально/на Cloud.ru.

## Ближайший шаг (предложение)
Начать с Волны 1, п.1–3 (429 Retry-After + валидатор пересечений + пустые Whisper-segments) —
подтверждены/дёшевы, снимают реальные P0/P1. Перед кодом — грепнуть VERIFY-пункты, написать
failing-тесты. Ждём отмашки на выбор пунктов.

## Ссылки на сырьё (в git не коммитим, tmp/)
- `tmp/pgpt_bundle.json` — сырой ответ `/api/snake3`.
- `tmp/pgpt_app.pretty.js` — beautified бандл (49 647 строк), основной источник цитат.
- `tmp/pgpt_app.html` / `.css` — обёртка.
- Пресеты конкурента: `<PremiereGPTBeta>/js/libs/render/*.epr`, `js/libs/mogrt/Texte PremiereGPT.mogrt`.
